// jest.setup.js
// Global setup for Jest tests (e.g., custom matchers, global mocks)
// Add any global setup here, e.g.:
// jest.setTimeout(30000);

// Example: extend expect with jest-extended if installed
// import 'jest-extended';
